# MIGRATION.md — smilefjes-parser

**GCP source repo:** `sondreskarsten/smilefjes-parser`
**Type:** parser
**Generated:** 2026-05-05

## Current GCP topology

| Field | Value |
|---|---|
| Pipeline (registry) | `parser` |
| Schedule (Oslo) | `07:35 Oslo` |
| Cloud Run Job | `smilefjes-parser` |
| Region | `europe-north1` |
| Artifact Registry image | `brreg-pipelines/smilefjes-parser` |
| Service account | `s1sfreracct@sondreskarsten-d7d14.iam.gserviceaccount.com` |

## AWS target topology

| Field | Value |
|---|---|
| Compute | ECS Fargate task |
| Region | `eu-north-1` |
| Image | `<account>.dkr.ecr.eu-north-1.amazonaws.com/smilefjes-parser:latest` |
| Schedule (UTC) | `cron(35 6 * * ? *)` |
| Trigger | EventBridge Scheduler `aws-scheduler:CreateSchedule` |
| IAM role | task role with policies for relevant S3 buckets + (Secrets Manager if used) |
| Logs | CloudWatch log group `/aws/ecs/smilefjes-parser` |

## Storage swap — gs:// → s3://

_No literal `gs://` paths in code (paths are constructed dynamically from env vars)._

## Environment variables

| Variable | First reference | Notes |
|---|---|---|
| `GCS_BUCKET` | `entrypoint.py:65` | Path/bucket variable — adjust for S3 layout |
| `GCS_PREFIX` | `entrypoint.py:66` | Path/bucket variable — adjust for S3 layout |
| `RUN_DATE` | `entrypoint.py:68` |  |
| `RUN_MODE` | `entrypoint.py:67` |  |
| `STATE_DIR` | `entrypoint.py:69` |  |

## GCP-specific imports (code swap surface)

| Import | First reference | AWS replacement |
|---|---|---|
| `from google.cloud import storage` | `resolve.py:47` | `boto3` |

## Container image

| Dockerfile | FROM | Notes |
|---|---|---|
| `Dockerfile` | `python:3.12-slim` |  |

## Manual checklist

- [ ] Build image and push to ECR
- [ ] Create ECS task definition with task role + S3 bucket policy
- [ ] Recreate every env var above as task definition env / Secrets Manager ref
- [ ] Replace GCP SA key with IAM task role
- [ ] Replace google.cloud.storage calls with boto3 (or use the env-driven layer if applicable)
- [ ] Create EventBridge schedule with the cron above
- [ ] Configure CloudWatch log group with retention
- [ ] Smoke-test by invoking the task once manually
- [ ] Verify outputs land in expected S3 prefixes
- [ ] Wire dependencies: ensure upstream tasks complete before this one runs

---

_This file is auto-generated by the manifest builder. Edit by hand only after confirming the generator no longer needs to re-run for this repo._
